# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for module `mdprof_dump'
# generated from source file `mdprof_dump.m'
# Generated by the Mercury compiler, version rotd-2017-06-13,
# configured for x86_64-apple-darwin13.4.0.

ifeq (x,y)
# Automatically generated dependency variables for module `mdprof_dump'
# generated from source file `mdprof_dump.m'
# Generated by the Mercury compiler, version rotd-2017-06-13,
# configured for x86_64-apple-darwin13.4.0.
endif

mdprof_dump.ms = \
	analysis_utils.m \
	apply_exclusion.m \
	array_util.m \
	builtin_modules.m \
	coverage.m \
	create_report.m \
	display.m \
	display_report.m \
	dump.m \
	exclude.m \
	feedback.m \
	feedback.automatic_parallelism.m \
	html_format.m \
	io_combinator.m \
	mdbcomp.m \
	mdbcomp.goal_path.m \
	mdprof_dump.m \
	measurement_units.m \
	measurements.m \
	message.m \
	prim_data.m \
	profile.m \
	program_representation.m \
	program_representation_utils.m \
	query.m \
	read_profile.m \
	recursion_patterns.m \
	report.m \
	rtti_access.m \
	shared_utilities.m \
	slice_and_dice.m \
	sym_name.m \
	top_procs.m \
	trace_counts.m \
	util.m \
	var_use_analysis.m
ifeq (x,y)
mdprof_dump.ms = \
	analysis_utils.m \
	apply_exclusion.m \
	array_util.m \
	builtin_modules.m \
	coverage.m \
	create_report.m \
	display.m \
	display_report.m \
	dump.m \
	exclude.m \
	feedback.m \
	feedback.automatic_parallelism.m \
	html_format.m \
	io_combinator.m \
	mdbcomp.m \
	mdbcomp.goal_path.m \
	mdprof_dump.m \
	measurement_units.m \
	measurements.m \
	message.m \
	prim_data.m \
	profile.m \
	program_representation.m \
	program_representation_utils.m \
	query.m \
	read_profile.m \
	recursion_patterns.m \
	report.m \
	rtti_access.m \
	shared_utilities.m \
	slice_and_dice.m \
	sym_name.m \
	top_procs.m \
	trace_counts.m \
	util.m \
	var_use_analysis.m
endif

mdprof_dump.errs = \
	analysis_utils.err \
	apply_exclusion.err \
	array_util.err \
	builtin_modules.err \
	coverage.err \
	create_report.err \
	display.err \
	display_report.err \
	dump.err \
	exclude.err \
	feedback.err \
	feedback.automatic_parallelism.err \
	html_format.err \
	io_combinator.err \
	mdbcomp.err \
	mdbcomp.goal_path.err \
	mdprof_dump.err \
	measurement_units.err \
	measurements.err \
	message.err \
	prim_data.err \
	profile.err \
	program_representation.err \
	program_representation_utils.err \
	query.err \
	read_profile.err \
	recursion_patterns.err \
	report.err \
	rtti_access.err \
	shared_utilities.err \
	slice_and_dice.err \
	sym_name.err \
	top_procs.err \
	trace_counts.err \
	util.err \
	var_use_analysis.err
ifeq (x,y)
mdprof_dump.errs = \
	analysis_utils.err \
	apply_exclusion.err \
	array_util.err \
	builtin_modules.err \
	coverage.err \
	create_report.err \
	display.err \
	display_report.err \
	dump.err \
	exclude.err \
	feedback.err \
	feedback.automatic_parallelism.err \
	html_format.err \
	io_combinator.err \
	mdbcomp.err \
	mdbcomp.goal_path.err \
	mdprof_dump.err \
	measurement_units.err \
	measurements.err \
	message.err \
	prim_data.err \
	profile.err \
	program_representation.err \
	program_representation_utils.err \
	query.err \
	read_profile.err \
	recursion_patterns.err \
	report.err \
	rtti_access.err \
	shared_utilities.err \
	slice_and_dice.err \
	sym_name.err \
	top_procs.err \
	trace_counts.err \
	util.err \
	var_use_analysis.err
endif

mdprof_dump.mods = \
	analysis_utils \
	apply_exclusion \
	array_util \
	coverage \
	create_report \
	display \
	display_report \
	dump \
	exclude \
	html_format \
	io_combinator \
	mdbcomp \
	mdbcomp.builtin_modules \
	mdbcomp.feedback \
	mdbcomp.feedback.automatic_parallelism \
	mdbcomp.goal_path \
	mdbcomp.prim_data \
	mdbcomp.program_representation \
	mdbcomp.rtti_access \
	mdbcomp.shared_utilities \
	mdbcomp.slice_and_dice \
	mdbcomp.sym_name \
	mdbcomp.trace_counts \
	mdprof_dump \
	measurement_units \
	measurements \
	message \
	profile \
	program_representation_utils \
	query \
	read_profile \
	recursion_patterns \
	report \
	top_procs \
	util \
	var_use_analysis
ifeq (x,y)
mdprof_dump.mods = \
	analysis_utils \
	apply_exclusion \
	array_util \
	coverage \
	create_report \
	display \
	display_report \
	dump \
	exclude \
	html_format \
	io_combinator \
	mdbcomp \
	mdbcomp.builtin_modules \
	mdbcomp.feedback \
	mdbcomp.feedback.automatic_parallelism \
	mdbcomp.goal_path \
	mdbcomp.prim_data \
	mdbcomp.program_representation \
	mdbcomp.rtti_access \
	mdbcomp.shared_utilities \
	mdbcomp.slice_and_dice \
	mdbcomp.sym_name \
	mdbcomp.trace_counts \
	mdprof_dump \
	measurement_units \
	measurements \
	message \
	profile \
	program_representation_utils \
	query \
	read_profile \
	recursion_patterns \
	report \
	top_procs \
	util \
	var_use_analysis
endif

mdprof_dump.parent_mods = \
	mdbcomp \
	mdbcomp.feedback
ifeq (x,y)
mdprof_dump.parent_mods = \
	mdbcomp \
	mdbcomp.feedback
endif

mdprof_dump.foreign =

ifeq (x,y)
mdprof_dump.foreign =
endif

mdprof_dump.foreign_cs = 
ifeq (x,y)
mdprof_dump.foreign_cs =
endif

mdprof_dump.foreign_dlls = $(mdprof_dump.foreign:%=$(dlls_subdir)%.dll)
ifeq (x,y)
mdprof_dump.foreign_dlls = $(mdprof_dump.foreign:%=$(dlls_subdir)%.dll)
endif

mdprof_dump.init_cs = $(mdprof_dump.mods:%=$(cs_subdir)%.c)
ifeq (x,y)
mdprof_dump.init_cs = $(mdprof_dump.mods:%=$(cs_subdir)%.c)
endif

mdprof_dump.cs = $(mdprof_dump.init_cs) 
ifeq (x,y)
mdprof_dump.cs = $(mdprof_dump.init_cs)
endif

mdprof_dump.dlls = $(mdprof_dump.mods:%=$(dlls_subdir)%.dll)
ifeq (x,y)
mdprof_dump.dlls = $(mdprof_dump.mods:%=$(dlls_subdir)%.dll)
endif

mdprof_dump.all_os = $(mdprof_dump.mods:%=$(os_subdir)%.$O)
ifeq (x,y)
mdprof_dump.all_os = $(mdprof_dump.mods:%=$(os_subdir)%.$O)
endif

mdprof_dump.all_pic_os = $(mdprof_dump.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))
ifeq (x,y)
mdprof_dump.all_pic_os = $(mdprof_dump.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))
endif

mdprof_dump.os = $(mdprof_dump.all_os)
ifeq (x,y)
mdprof_dump.os = $(mdprof_dump.all_os)
endif

mdprof_dump.pic_os = $(mdprof_dump.all_pic_os)
ifeq (x,y)
mdprof_dump.pic_os = $(mdprof_dump.all_pic_os)
endif

mdprof_dump.useds = $(mdprof_dump.mods:%=$(useds_subdir)%.used)
ifeq (x,y)
mdprof_dump.useds = $(mdprof_dump.mods:%=$(used_subdir)%.used)
endif

mdprof_dump.javas = $(mdprof_dump.mods:%=$(javas_subdir)%.java)
ifeq (x,y)
mdprof_dump.javas = $(mdprof_dump.mods:%=$(javas_subdir)%.java)
endif

mdprof_dump.classes = $(mdprof_dump.mods:%=$(classes_subdir)%.class) $(wildcard $(mdprof_dump.mods:%=$(classes_subdir)%\$$*.class))
ifeq (x,y)
mdprof_dump.classes = \
	$(mdprof_dump.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mdprof_dump.mods:%=$(classes_subdir)%\$$*.class))
endif

mdprof_dump.dirs = $(mdprof_dump.mods:%=$(dirs_subdir)%.dir)
ifeq (x,y)
mdprof_dump.dirs = $(mdprof_dump.mods:%=$(dirs_subdir)%.dir)
endif

mdprof_dump.dir_os = $(mdprof_dump.mods:%=$(dirs_subdir)%.dir/*.$O)
ifeq (x,y)
mdprof_dump.dir_os = $(mdprof_dump.mods:%=$(dirs_subdir)%.dir/*.$O)
endif

mdprof_dump.dates = $(mdprof_dump.mods:%=$(dates_subdir)%.date)
ifeq (x,y)
mdprof_dump.dates = $(mdprof_dump.mods:%=$(dates_subdir)%.date)
endif

mdprof_dump.date0s = $(mdprof_dump.mods:%=$(date0s_subdir)%.date0)
ifeq (x,y)
mdprof_dump.date0s = $(mdprof_dump.mods:%=$(date0s_subdir)%.date0)
endif

mdprof_dump.date3s = $(mdprof_dump.mods:%=$(date3s_subdir)%.date3)
ifeq (x,y)
mdprof_dump.date3s = $(mdprof_dump.mods:%=$(date3s_subdir)%.date3)
endif

mdprof_dump.optdates = $(mdprof_dump.mods:%=$(optdates_subdir)%.optdate)
ifeq (x,y)
mdprof_dump.optdates = $(mdprof_dump.mods:%=$(optdates_subdir)%.optdate)
endif

mdprof_dump.trans_opt_dates = $(mdprof_dump.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)
ifeq (x,y)
mdprof_dump.trans_opt_dates = $(mdprof_dump.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)
endif

mdprof_dump.c_dates = $(mdprof_dump.mods:%=$(c_dates_subdir)%.c_date)
ifeq (x,y)
mdprof_dump.c_dates = $(mdprof_dump.mods:%=$(c_dates_subdir)%.c_date)
endif

mdprof_dump.java_dates = $(mdprof_dump.mods:%=$(java_dates_subdir)%.java_date)
ifeq (x,y)
mdprof_dump.java_dates = $(mdprof_dump.mods:%=$(java_dates_subdir)%.java_date)
endif

mdprof_dump.ds = $(mdprof_dump.mods:%=$(ds_subdir)%.d)
ifeq (x,y)
mdprof_dump.ds = $(mdprof_dump.mods:%=$(ds_subdir)%.d)
endif

mdprof_dump.module_deps = $(mdprof_dump.mods:%=$(module_deps_subdir)%.module_dep)
ifeq (x,y)
mdprof_dump.module_deps = $(mdprof_dump.mods:%=$(module_deps_subdir)%.module_dep)
endif

mdprof_dump.mihs = $(mdprof_dump.mods:%=$(mihs_subdir)%.mih)
ifeq (x,y)
mdprof_dump.mihs = $(mdprof_dump.mods:%=$(mihs_subdir)%.mih)
endif

mdprof_dump.mhs = $(mdprof_dump.mods:%=%.mh)
ifeq (x,y)
mdprof_dump.mhs = $(mdprof_dump.mods:%=%.mh)
endif

mdprof_dump.all_mihs = $(mdprof_dump.mods:%=$(mihs_subdir)%.mih)
ifeq (x,y)
mdprof_dump.all_mihs = $(mdprof_dump.mods:%=$(mihs_subdir)%.mih)
endif

mdprof_dump.all_mhs = $(mdprof_dump.mods:%=%.mh)
ifeq (x,y)
mdprof_dump.all_mhs = $(mdprof_dump.mods:%=%.mh)
endif

mdprof_dump.ints = $(mdprof_dump.mods:%=$(ints_subdir)%.int) $(mdprof_dump.mods:%=$(int2s_subdir)%.int2)
ifeq (x,y)
mdprof_dump.ints = \
	$(mdprof_dump.mods:%=$(ints_subdir)%.int) \
	$(mdprof_dump.mods:%=$(int2s_subdir)%.int2)
endif

mdprof_dump.int0s = $(mdprof_dump.parent_mods:%=$(int0s_subdir)%.int0)
ifeq (x,y)
mdprof_dump.int0s = $(mdprof_dump.parent_mods:%=$(int0s_subdir)%.int0)
endif

mdprof_dump.all_int0s = $(mdprof_dump.mods:%=$(int0s_subdir)%.int0)
ifeq (x,y)
mdprof_dump.all_int0s = $(mdprof_dump.mods:%=$(int0s_subdir)%.int0)
endif

mdprof_dump.int3s = $(mdprof_dump.mods:%=$(int3s_subdir)%.int3)
ifeq (x,y)
mdprof_dump.int3s = $(mdprof_dump.mods:%=$(int3s_subdir)%.int3)
endif

mdprof_dump.opts = $(mdprof_dump.mods:%=$(opts_subdir)%.opt)
ifeq (x,y)
mdprof_dump.opts = $(mdprof_dump.mods:%=$(opts_subdir)%.opt)
endif

mdprof_dump.trans_opts = $(mdprof_dump.mods:%=$(trans_opts_subdir)%.trans_opt)
ifeq (x,y)
mdprof_dump.trans_opts = $(mdprof_dump.mods:%=$(trans_opts_subdir)%.trans_opt)
endif

mdprof_dump.analysiss = $(mdprof_dump.mods:%=$(analysiss_subdir)%.analysis)
ifeq (x,y)
mdprof_dump.analysiss = $(mdprof_dump.mods:%=$(analysiss_subdir)%.analysis)
endif

mdprof_dump.requests = $(mdprof_dump.mods:%=$(requests_subdir)%.request)
ifeq (x,y)
mdprof_dump.requests = $(mdprof_dump.mods:%=$(requests_subdir)%.request)
endif

mdprof_dump.imdgs = $(mdprof_dump.mods:%=$(imdgs_subdir)%.imdg)
ifeq (x,y)
mdprof_dump.imdgs = $(mdprof_dump.mods:%=$(imdgs_subdir)%.imdg)
endif

mdprof_dump.profs = $(mdprof_dump.mods:%=%.prof)

ifeq (x,y)
mdprof_dump.profs = $(mdprof_dump.mods:%=%.prof)
endif

