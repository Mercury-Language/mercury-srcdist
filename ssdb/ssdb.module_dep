module(1, "ssdb.m",
	ssdb,
	{},
	{builtin, io, list, private_builtin},
	{builtin, io, list, assoc_list, bitmap, bool, char, dir, int, map, maybe, mdb, pair, require, string, univ, (mdb.browse), (mdb.browser_info), (mdb.browser_term), (mdb.listing), private_builtin, (string.format), (string.parse_util)},
	{},
	{},
	{},
	{"C"},
	{"C" - ssdb, "C#" - ssdb, "Java" - ssdb, "IL" - ssdb, "Erlang" - ssdb, "C" - ssdb},
	contains_foreign_export,
	no_main
).
