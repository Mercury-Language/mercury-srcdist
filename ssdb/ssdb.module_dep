module(1, "ssdb.m",
	ssdb,
	{},
	{builtin, io, list, private_builtin},
	{assoc_list, bitmap, bool, builtin, char, dir, int, io, list, map, maybe, mdb, pair, private_builtin, require, stream, string, univ, (mdb.browse), (mdb.browser_info), (mdb.browser_term), (mdb.listing), (string.format), (string.parse_util)},
	{},
	{},
	{},
	{"C"},
	{"C" - ssdb, "C#" - ssdb, "Java" - ssdb, "IL" - ssdb, "Erlang" - ssdb},
	contains_foreign_export,
	no_main
).
