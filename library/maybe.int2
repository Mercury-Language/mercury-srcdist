:- module maybe.
:- interface.
:- type maybe_error
	--->	ok 
	;	error(string).
:- type maybe(T)
	--->	no 
	;	yes(T).
:- type maybe_error(T)
	--->	ok(T)
	;	error(string).
:- inst (maybe(I)) == bound(no ; yes(I)).
:- inst (maybe_error(I)) == bound(error(ground) ; ok(I)).
