:- module test_bitset.
:- interface.
:- type bitset_error(T)
    --->    zero_argument(
                 string,
                 test_bitset.test_bitset(T)
            )
    ;       one_argument(
                 string,
                 test_bitset.test_bitset(T),
                 test_bitset.test_bitset(T)
            )
    ;       two_arguments(
                 string,
                 test_bitset.test_bitset(T),
                 test_bitset.test_bitset(T),
                 test_bitset.test_bitset(T)
            ).
:- type test_bitset(T).
