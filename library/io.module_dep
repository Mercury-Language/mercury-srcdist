module(1, "io.m",
	io,
	{},
	{builtin, bitmap, bool, char, deconstruct, list, map, maybe, ops, stream, string, time, univ, private_builtin},
	{builtin, array, benchmarking, bitmap, bool, char, deconstruct, dir, enum, exception, int, list, map, maybe, ops, parser, require, stream, string, term, term_conversion, term_io, time, type_desc, univ, (stream.string_writer), private_builtin, (string.format), (string.parse_util), rtti_implementation, table_builtin},
	{},
	{},
	{},
	{"C"},
	{"C" - io, "C#" - io, "Java" - io, "IL" - io, "Erlang" - io, "C" - io, "C" - time, "C" - string},
	contains_foreign_export,
	no_main
).
