module(1, "stream.string_writer.m",
	(stream.string_writer),
	{stream},
	{builtin, char, deconstruct, io, list, ops, private_builtin, string, univ},
	{array, bitmap, builtin, calendar, char, deconstruct, int, integer, io, list, ops, private_builtin, require, rtti_implementation, stream, string, term_io, type_desc, univ, version_array, (string.format), (string.parse_util)},
	{},
	{},
	{},
	{},
	{},
	no_foreign_export,
	no_main
).
