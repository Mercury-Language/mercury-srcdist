:- module version_hash_table.
:- interface.
:- type hash_pred(K) == pred(K, int).
:- type version_hash_table(K, V).
:- inst (hash_pred) == (pred((builtin.in), (builtin.out)) is det).
:- type_representation(buckets(V_1, V_2), is_eqv_to(version_array.version_array(version_hash_table.hash_table_alist(V_1, V_2)))).
:- type_representation(hash_pred(V_1), is_eqv_to(pred(V_1, int))).
