module(1, "library.m",
	library,
	{},
	{builtin, private_builtin},
	{builtin, array, array2d, assoc_list, backjump, bag, benchmarking, bimap, bintree, bintree_set, bitmap, bit_buffer, (bit_buffer.read), (bit_buffer.write), bool, bt_array, builtin, char, construct, cord, counter, deconstruct, digraph, dir, enum, eqvclass, exception, float, gc, getopt, getopt_io, graph, group, hash_table, injection, int, integer, io, lexer, list, map, math, maybe, multi_map, ops, pair, parser, pprint, pqueue, pretty_printer, prolog, queue, random, rational, rbtree, relation, require, robdd, rtree, set, set_bbbtree, set_ctree234, set_ordlist, set_tree234, set_unordlist, solutions, sparse_bitset, stack, std_util, store, stream, (stream.string_writer), string, (string.builder), svarray, svbag, svbimap, sveqvclass, svmap, svmulti_map, svqueue, svrelation, svset, svvarset, table_statistics, term, term_io, term_to_xml, thread, time, tree234, tree_bitset, type_desc, unit, univ, varset, version_array, version_array2d, version_bitmap, version_hash_table, version_store, version_types, erlang_builtin, erlang_rtti_implementation, mutvar, par_builtin, private_builtin, profiling_builtin, region_builtin, rtti_implementation, stm_builtin, table_builtin, term_size_prof_builtin, private_builtin},
	{},
	{},
	{},
	{"C"},
	{"C" - library, "C#" - library, "Java" - library, "Erlang" - library, "C" - library},
	no_foreign_export,
	no_main
).
