# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for module `mfilterjavac'
# generated from source file `mfilterjavac.m'
# Generated by the Mercury compiler, version rotd-2017-06-12,
# configured for x86_64-apple-darwin13.4.0.

ifeq (x,y)
# Automatically generated dependency variables for module `mfilterjavac'
# generated from source file `mfilterjavac.m'
# Generated by the Mercury compiler, version rotd-2017-06-12,
# configured for x86_64-apple-darwin13.4.0.
endif

mfilterjavac.ms = \
	mfilterjavac.m
ifeq (x,y)
mfilterjavac.ms = mfilterjavac.m
endif

mfilterjavac.errs = \
	mfilterjavac.err
ifeq (x,y)
mfilterjavac.errs = mfilterjavac.err
endif

mfilterjavac.mods = \
	mfilterjavac
ifeq (x,y)
mfilterjavac.mods = mfilterjavac
endif

mfilterjavac.parent_mods =
ifeq (x,y)
mfilterjavac.parent_mods =
endif

mfilterjavac.foreign =

ifeq (x,y)
mfilterjavac.foreign =
endif

mfilterjavac.foreign_cs = 
ifeq (x,y)
mfilterjavac.foreign_cs =
endif

mfilterjavac.foreign_dlls = $(mfilterjavac.foreign:%=$(dlls_subdir)%.dll)
ifeq (x,y)
mfilterjavac.foreign_dlls = $(mfilterjavac.foreign:%=$(dlls_subdir)%.dll)
endif

mfilterjavac.init_cs = $(mfilterjavac.mods:%=$(cs_subdir)%.c)
ifeq (x,y)
mfilterjavac.init_cs = $(mfilterjavac.mods:%=$(cs_subdir)%.c)
endif

mfilterjavac.cs = $(mfilterjavac.init_cs) 
ifeq (x,y)
mfilterjavac.cs = $(mfilterjavac.init_cs)
endif

mfilterjavac.dlls = $(mfilterjavac.mods:%=$(dlls_subdir)%.dll)
ifeq (x,y)
mfilterjavac.dlls = $(mfilterjavac.mods:%=$(dlls_subdir)%.dll)
endif

mfilterjavac.all_os = $(mfilterjavac.mods:%=$(os_subdir)%.$O)
ifeq (x,y)
mfilterjavac.all_os = $(mfilterjavac.mods:%=$(os_subdir)%.$O)
endif

mfilterjavac.all_pic_os = $(mfilterjavac.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))
ifeq (x,y)
mfilterjavac.all_pic_os = $(mfilterjavac.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))
endif

mfilterjavac.os = $(mfilterjavac.all_os)
ifeq (x,y)
mfilterjavac.os = $(mfilterjavac.all_os)
endif

mfilterjavac.pic_os = $(mfilterjavac.all_pic_os)
ifeq (x,y)
mfilterjavac.pic_os = $(mfilterjavac.all_pic_os)
endif

mfilterjavac.useds = $(mfilterjavac.mods:%=$(useds_subdir)%.used)
ifeq (x,y)
mfilterjavac.useds = $(mfilterjavac.mods:%=$(used_subdir)%.used)
endif

mfilterjavac.javas = $(mfilterjavac.mods:%=$(javas_subdir)%.java)
ifeq (x,y)
mfilterjavac.javas = $(mfilterjavac.mods:%=$(javas_subdir)%.java)
endif

mfilterjavac.classes = $(mfilterjavac.mods:%=$(classes_subdir)%.class) $(wildcard $(mfilterjavac.mods:%=$(classes_subdir)%\$$*.class))
ifeq (x,y)
mfilterjavac.classes = \
	$(mfilterjavac.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mfilterjavac.mods:%=$(classes_subdir)%\$$*.class))
endif

mfilterjavac.dirs = $(mfilterjavac.mods:%=$(dirs_subdir)%.dir)
ifeq (x,y)
mfilterjavac.dirs = $(mfilterjavac.mods:%=$(dirs_subdir)%.dir)
endif

mfilterjavac.dir_os = $(mfilterjavac.mods:%=$(dirs_subdir)%.dir/*.$O)
ifeq (x,y)
mfilterjavac.dir_os = $(mfilterjavac.mods:%=$(dirs_subdir)%.dir/*.$O)
endif

mfilterjavac.dates = $(mfilterjavac.mods:%=$(dates_subdir)%.date)
ifeq (x,y)
mfilterjavac.dates = $(mfilterjavac.mods:%=$(dates_subdir)%.date)
endif

mfilterjavac.date0s = $(mfilterjavac.mods:%=$(date0s_subdir)%.date0)
ifeq (x,y)
mfilterjavac.date0s = $(mfilterjavac.mods:%=$(date0s_subdir)%.date0)
endif

mfilterjavac.date3s = $(mfilterjavac.mods:%=$(date3s_subdir)%.date3)
ifeq (x,y)
mfilterjavac.date3s = $(mfilterjavac.mods:%=$(date3s_subdir)%.date3)
endif

mfilterjavac.optdates = $(mfilterjavac.mods:%=$(optdates_subdir)%.optdate)
ifeq (x,y)
mfilterjavac.optdates = $(mfilterjavac.mods:%=$(optdates_subdir)%.optdate)
endif

mfilterjavac.trans_opt_dates = $(mfilterjavac.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)
ifeq (x,y)
mfilterjavac.trans_opt_dates = $(mfilterjavac.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)
endif

mfilterjavac.c_dates = $(mfilterjavac.mods:%=$(c_dates_subdir)%.c_date)
ifeq (x,y)
mfilterjavac.c_dates = $(mfilterjavac.mods:%=$(c_dates_subdir)%.c_date)
endif

mfilterjavac.java_dates = $(mfilterjavac.mods:%=$(java_dates_subdir)%.java_date)
ifeq (x,y)
mfilterjavac.java_dates = $(mfilterjavac.mods:%=$(java_dates_subdir)%.java_date)
endif

mfilterjavac.ds = $(mfilterjavac.mods:%=$(ds_subdir)%.d)
ifeq (x,y)
mfilterjavac.ds = $(mfilterjavac.mods:%=$(ds_subdir)%.d)
endif

mfilterjavac.module_deps = $(mfilterjavac.mods:%=$(module_deps_subdir)%.module_dep)
ifeq (x,y)
mfilterjavac.module_deps = $(mfilterjavac.mods:%=$(module_deps_subdir)%.module_dep)
endif

mfilterjavac.mihs = $(mfilterjavac.mods:%=$(mihs_subdir)%.mih)
ifeq (x,y)
mfilterjavac.mihs = $(mfilterjavac.mods:%=$(mihs_subdir)%.mih)
endif

mfilterjavac.mhs = $(mfilterjavac.mods:%=%.mh)
ifeq (x,y)
mfilterjavac.mhs = $(mfilterjavac.mods:%=%.mh)
endif

mfilterjavac.all_mihs = $(mfilterjavac.mods:%=$(mihs_subdir)%.mih)
ifeq (x,y)
mfilterjavac.all_mihs = $(mfilterjavac.mods:%=$(mihs_subdir)%.mih)
endif

mfilterjavac.all_mhs = $(mfilterjavac.mods:%=%.mh)
ifeq (x,y)
mfilterjavac.all_mhs = $(mfilterjavac.mods:%=%.mh)
endif

mfilterjavac.ints = $(mfilterjavac.mods:%=$(ints_subdir)%.int) $(mfilterjavac.mods:%=$(int2s_subdir)%.int2)
ifeq (x,y)
mfilterjavac.ints = \
	$(mfilterjavac.mods:%=$(ints_subdir)%.int) \
	$(mfilterjavac.mods:%=$(int2s_subdir)%.int2)
endif

mfilterjavac.int0s = $(mfilterjavac.parent_mods:%=$(int0s_subdir)%.int0)
ifeq (x,y)
mfilterjavac.int0s = $(mfilterjavac.parent_mods:%=$(int0s_subdir)%.int0)
endif

mfilterjavac.all_int0s = $(mfilterjavac.mods:%=$(int0s_subdir)%.int0)
ifeq (x,y)
mfilterjavac.all_int0s = $(mfilterjavac.mods:%=$(int0s_subdir)%.int0)
endif

mfilterjavac.int3s = $(mfilterjavac.mods:%=$(int3s_subdir)%.int3)
ifeq (x,y)
mfilterjavac.int3s = $(mfilterjavac.mods:%=$(int3s_subdir)%.int3)
endif

mfilterjavac.opts = $(mfilterjavac.mods:%=$(opts_subdir)%.opt)
ifeq (x,y)
mfilterjavac.opts = $(mfilterjavac.mods:%=$(opts_subdir)%.opt)
endif

mfilterjavac.trans_opts = $(mfilterjavac.mods:%=$(trans_opts_subdir)%.trans_opt)
ifeq (x,y)
mfilterjavac.trans_opts = $(mfilterjavac.mods:%=$(trans_opts_subdir)%.trans_opt)
endif

mfilterjavac.analysiss = $(mfilterjavac.mods:%=$(analysiss_subdir)%.analysis)
ifeq (x,y)
mfilterjavac.analysiss = $(mfilterjavac.mods:%=$(analysiss_subdir)%.analysis)
endif

mfilterjavac.requests = $(mfilterjavac.mods:%=$(requests_subdir)%.request)
ifeq (x,y)
mfilterjavac.requests = $(mfilterjavac.mods:%=$(requests_subdir)%.request)
endif

mfilterjavac.imdgs = $(mfilterjavac.mods:%=$(imdgs_subdir)%.imdg)
ifeq (x,y)
mfilterjavac.imdgs = $(mfilterjavac.mods:%=$(imdgs_subdir)%.imdg)
endif

mfilterjavac.profs = $(mfilterjavac.mods:%=%.prof)

ifeq (x,y)
mfilterjavac.profs = $(mfilterjavac.mods:%=%.prof)
endif

