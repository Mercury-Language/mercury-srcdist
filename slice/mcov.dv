# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for
# module `mcov' in source file `mcov.m'.
# Generated by Mercury compiler version rotd-2025-10-14
# configured for x86_64-pc-linux-gnu.

mcov.ms = \
	builtin_modules.m \
	feedback.m \
	feedback.automatic_parallelism.m \
	feedback.feedback_info.m \
	mcov.m \
	mdbcomp.m \
	mdbcomp.goal_path.m \
	prim_data.m \
	program_representation.m \
	read_trace_counts.m \
	rtti_access.m \
	shared_utilities.m \
	slice_and_dice.m \
	sym_name.m \
	trace_counts.m \
	write_trace_counts.m

mcov.dep_errs = \
	builtin_modules.dep_err \
	feedback.dep_err \
	feedback.automatic_parallelism.dep_err \
	feedback.feedback_info.dep_err \
	mcov.dep_err \
	mdbcomp.dep_err \
	mdbcomp.goal_path.dep_err \
	prim_data.dep_err \
	program_representation.dep_err \
	read_trace_counts.dep_err \
	rtti_access.dep_err \
	shared_utilities.dep_err \
	slice_and_dice.dep_err \
	sym_name.dep_err \
	trace_counts.dep_err \
	write_trace_counts.dep_err

mcov.errs = \
	builtin_modules.err \
	feedback.err \
	feedback.automatic_parallelism.err \
	feedback.feedback_info.err \
	mcov.err \
	mdbcomp.err \
	mdbcomp.goal_path.err \
	prim_data.err \
	program_representation.err \
	read_trace_counts.err \
	rtti_access.err \
	shared_utilities.err \
	slice_and_dice.err \
	sym_name.err \
	trace_counts.err \
	write_trace_counts.err

mcov.mods = \
	mcov \
	mdbcomp \
	mdbcomp.builtin_modules \
	mdbcomp.feedback \
	mdbcomp.feedback.automatic_parallelism \
	mdbcomp.feedback.feedback_info \
	mdbcomp.goal_path \
	mdbcomp.prim_data \
	mdbcomp.program_representation \
	mdbcomp.read_trace_counts \
	mdbcomp.rtti_access \
	mdbcomp.shared_utilities \
	mdbcomp.slice_and_dice \
	mdbcomp.sym_name \
	mdbcomp.trace_counts \
	mdbcomp.write_trace_counts

mcov.parent_mods = \
	mdbcomp \
	mdbcomp.feedback

mcov.ds = $(mcov.mods:%=$(ds_subdir)%.d)

mcov.module_deps = $(mcov.mods:%=$(module_deps_subdir)%.module_dep)

mcov.profs = $(mcov.mods:%=%.prof)

mcov.int1s = $(mcov.mods:%=$(int1s_subdir)%.int)

mcov.int2s = $(mcov.mods:%=$(int2s_subdir)%.int2)

mcov.ints = \
	$(mcov.mods:%=$(ints_subdir)%.int) \
	$(mcov.mods:%=$(int2s_subdir)%.int2)

mcov.int0s = $(mcov.parent_mods:%=$(int0s_subdir)%.int0)

mcov.int0s_to_clean = $(mcov.mods:%=$(int0s_subdir)%.int0)

mcov.all_int0s = $(mcov.mods:%=$(int0s_subdir)%.int0)

mcov.int3s = $(mcov.mods:%=$(int3s_subdir)%.int3)

mcov.dates = $(mcov.mods:%=$(dates_subdir)%.date)

mcov.date0s = $(mcov.mods:%=$(date0s_subdir)%.date0)

mcov.date3s = $(mcov.mods:%=$(date3s_subdir)%.date3)

mcov.all_opts = $(mcov.mods:%=$(opts_subdir)%.opt)

mcov.opts = $(mcov.mods:%=$(opts_subdir)%.opt)

mcov.all_trans_opts = $(mcov.mods:%=$(trans_opts_subdir)%.trans_opt)

mcov.trans_opts = $(mcov.mods:%=$(trans_opts_subdir)%.trans_opt)

mcov.optdates = $(mcov.mods:%=$(optdates_subdir)%.optdate)

mcov.trans_opt_dates = $(mcov.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

mcov.all_cs = $(mcov.mods:%=$(cs_subdir)%.c)

mcov.init_cs = $(mcov.mods:%=$(cs_subdir)%.c)

mcov.c_dates = $(mcov.mods:%=$(c_dates_subdir)%.c_date)

mcov.all_os = $(mcov.mods:%=$(os_subdir)%.$O)

mcov.all_pic_os = $(mcov.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

mcov.foreign =

mcov.foreign_cs =

mcov.mihs = $(mcov.mods:%=$(mihs_subdir)%.mih)

mcov.mhs = $(mcov.mods:%=$(mhs_subdir)%.mh)

mcov.mihs_to_clean = $(mcov.mods:%=$(mihs_subdir)%.mih)

mcov.all_mihs = $(mcov.mods:%=$(mihs_subdir)%.mih)

mcov.mhs_to_clean = $(mcov.mods:%=$(mhs_subdir)%.mh)

mcov.all_mhs = $(mcov.mods:%=$(mhs_subdir)%.mh)

mcov.all_javas = $(mcov.mods:%=$(javas_subdir)%.java)

mcov.java_dates = $(mcov.mods:%=$(java_dates_subdir)%.java_date)

ifeq ($(findstring java,$(GRADE)),java)

mcov.classes = \
	$(mcov.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mcov.mods:%=$(classes_subdir)%\$$*.class))

else

mcov.classes = 

endif # conditional fragment

mcov.all_css = $(mcov.mods:%=$(css_subdir)%.cs)

mcov.cs_dates = $(mcov.mods:%=$(cs_dates_subdir)%.cs_date)

mcov.dlls = $(mcov.mods:%=$(dlls_subdir)%.dll)

mcov.foreign_dlls = $(mcov.foreign:%=$(dlls_subdir)%.dll)

mcov.useds = $(mcov.mods:%=$(useds_subdir)%.used)

mcov.analyses = $(mcov.mods:%=$(analyses_subdir)%.analysis)

mcov.requests = $(mcov.mods:%=$(requests_subdir)%.request)

mcov.imdgs = $(mcov.mods:%=$(imdgs_subdir)%.imdg)

