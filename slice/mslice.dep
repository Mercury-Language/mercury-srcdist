# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated program dependencies for
# module `mslice' in source file `mslice.m'.
# Generated by Mercury compiler version rotd-2024-10-25
# configured for x86_64-pc-linux-gnu.

ifneq ($(EXT_FOR_EXE),)

# RULE ext_for_exe
.PHONY: mslice
mslice : mslice$(EXT_FOR_EXE)

endif # conditional fragment

ifeq ($(findstring java,$(GRADE)),java)

# RULE executable_java
mslice : $(mslice.classes)

else

# RULE executable_non_java
mslice$(EXT_FOR_EXE) : \
		$(mslice.all_os) \
		mslice_init.$O \
		$(foreach @,mslice,$(ALL_MLOBJS)) \
		$(foreach @,mslice,$(ALL_MLLIBS_DEP))
	$(ML) $(ALL_GRADEFLAGS) $(ALL_MLFLAGS) -- $(ALL_LDFLAGS) $(EXEFILE_OPT)mslice$(EXT_FOR_EXE) mslice_init.$O \
		$(mslice.all_os) \
		$(foreach @,mslice,$(ALL_MLOBJS)) $(ALL_MLLIBS)

endif # conditional fragment

ifeq ($(findstring java,$(GRADE)),java)

# RULE lib_target_java
.PHONY: libmslice
libmslice : \
		mslice.jar \
		$(mslice.ints) \
		$(mslice.int3s) \
		mslice.init

else

# RULE lib_target_non_java
.PHONY: libmslice
libmslice : \
		libmslice.$A \
		libmslice.$(EXT_FOR_SHARED_LIB) \
		$(mslice.ints) \
		$(mslice.int3s) \
		mslice.init

endif # conditional fragment

ifneq ($(EXT_FOR_SHARED_LIB),$(A))

# RULE shared_lib
libmslice.$(EXT_FOR_SHARED_LIB) : \
		$(mslice.all_pic_os) \
		$(patsubst %.o,%.$(EXT_FOR_PIC_OBJECTS),$(foreach @,mslice,$(ALL_MLOBJS))) \
		$(foreach @,mslice,$(ALL_MLLIBS_DEP))
	$(ML) --make-shared-lib $(ALL_GRADEFLAGS) $(ALL_MLFLAGS) --  $(ALL_LD_LIBFLAGS) -o libmslice.$(EXT_FOR_SHARED_LIB) \
		$(mslice.all_pic_os) \
		$(patsubst %.o,%.$(EXT_FOR_PIC_OBJECTS),$(foreach @,mslice,$(ALL_MLOBJS))) $(ALL_MLLIBS)

endif # conditional fragment

# RULE lib
libmslice.$A : \
		$(mslice.all_os) \
		$(foreach @,mslice,$(ALL_MLOBJS))
	rm -f libmslice.$A
	$(AR) $(ALL_ARFLAGS) $(AR_LIBFILE_OPT)libmslice.$A $(mslice.all_os) \
		$(foreach @,mslice,$(ALL_MLOBJS))
	$(RANLIB) $(ALL_RANLIBFLAGS) libmslice.$A

# RULE jar
mslice.jar : $(mslice.classes)
	$(JAR) $(JAR_CREATE_FLAGS) mslice.jar $(mslice.classes)

# RULE init_file
mslice.init : \
		mslice.dep \
		$(mslice.all_cs)
	echo > mslice.init
	$(MKLIBINIT) $(mslice.all_cs) >> mslice.init
	$(EXTRA_INIT_COMMAND) >> mslice.init

# RULE force_init_c_file
force-mslice_init :

# RULE init_c_file
mslice_init.c : \
		force-mslice_init \
		$(mslice.all_cs)
	@$(C2INIT) $(ALL_GRADEFLAGS) $(ALL_C2INITFLAGS) --init-c-file mslice_init.c.tmp $(mslice.init_cs) $(ALL_C2INITARGS)
	@mercury_update_interface mslice_init.c

# RULE lib_install_ints
.PHONY: libmslice.install_ints
libmslice.install_ints : \
		$(mslice.ints) \
		$(mslice.int3s) \
		$(mslice.int0s) \
		install_lib_dirs
	files="$(mslice.ints) $(mslice.int3s) $(mslice.int0s) "; \
	for file in $$files; do \
		target="$(INSTALL_INT_DIR)/`basename $$file`"; \
		if cmp -s "$$file" "$$target"; then \
			echo "$$target unchanged"; \
		else \
			echo "installing $$target"; \
			$(INSTALL) "$$file" "$$target"; \
		fi; \
	done
	# The following is needed to support the `--use-subdirs' option.
	# We try using `$(LN_S)', but if that fails, then we just use
	# `$(INSTALL)'.
	for ext in mh int int2 int3 int0; do \
		dir="$(INSTALL_INT_DIR)/Mercury/$${ext}s"; \
		rm -rf "$$dir"; \
		$(LN_S) .. "$$dir" || { \
			{ test -d "$$dir" || \
			$(INSTALL_MKDIR) "$$dir"; } && \
			$(INSTALL) "$(INSTALL_INT_DIR)"/*.$$ext "$$dir"; \
		} || exit 1; \
	done

# RULE lib_install_opts
.PHONY: libmslice.install_opts
libmslice.install_opts :
	@:

ifeq ($(mslice.mhs),)

# RULE install_lib_hdrs_nomhs
.PHONY: libmslice.install_hdrs
libmslice.install_hdrs : \
		$(mslice.mhs) \
		install_lib_dirs
	@:

else

# RULE install_lib_hdrs_mhs
.PHONY: libmslice.install_hdrs
libmslice.install_hdrs : \
		$(mslice.mhs) \
		install_lib_dirs
	for hdr in $(mslice.mhs); do \
		$(INSTALL) $$hdr $(INSTALL_INT_DIR); \
		$(INSTALL) $$hdr $(INSTALL_INC_DIR); \
	done

endif # conditional fragment

ifeq ($(mslice.mihs),)

# RULE install_grade_hdrs_no_mihs
.PHONY: libmslice.install_grade_hdrs
libmslice.install_grade_hdrs : \
		$(mslice.mihs) \
		install_grade_dirs
	@:

else

# RULE install_grade_hdrs_mihs
.PHONY: libmslice.install_grade_hdrs
libmslice.install_grade_hdrs : \
		$(mslice.mihs) \
		install_grade_dirs
	for hdr in $(mslice.mihs); do \
		$(INSTALL) $$hdr $(INSTALL_INT_DIR); \
		$(INSTALL) $$hdr $(INSTALL_GRADE_INC_DIR); \
	done
	# The following is needed to support the `--use-subdirs' option.
	# We try using `$(LN_S)', but if that fails, then we just use
	# `$(INSTALL)'.
	rm -rf $(INSTALL_GRADE_INC_SUBDIR)
	$(LN_S) .. $(INSTALL_GRADE_INC_SUBDIR) || { \
		{ test -d $(INSTALL_GRADE_INC_SUBDIR) || \
			$(INSTALL_MKDIR) $(INSTALL_GRADE_INC_SUBDIR); \
		} && \
		$(INSTALL) $(INSTALL_GRADE_INC_DIR)/*.mih \
			$(INSTALL_GRADE_INC_SUBDIR); \
	} || exit 1
	rm -rf $(INSTALL_INT_DIR)/Mercury/mihs
	$(LN_S) .. $(INSTALL_INT_DIR)/Mercury/mihs || { \
		{ test -d $(INSTALL_INT_DIR)/Mercury/mihs || \
			$(INSTALL_MKDIR) \
			$(INSTALL_INT_DIR)/Mercury/mihs; \
		} && \
		$(INSTALL) $(INSTALL_GRADE_INC_DIR)/*.mih \
			$(INSTALL_INT_DIR); \
	} || exit 1

endif # conditional fragment

# RULE install__all_files
.PHONY: libmslice.install_all_files
libmslice.install_all_files : libmslice.install_ngs_pgs_files
	rm -rf tmp_dir && \
	mkdir tmp_dir && \
	rm -rf tmp_dir && \
	mkdir tmp_dir && \
	grade_files="$(foreach e,$(GRADE_SUBDIR_MVEXTS),$(mslice.$(e)))" && \
	for file in x $$grade_files; do \
		if test "$$file" != x; then \
			mv -f $$file tmp_dir > /dev/null 2>&1; \
			true; \
		fi; \
	done && \
	{ mv -f $(deps_subdir)$*.dep $(deps_subdir)$*.dv \
		*.$A *.$(EXT_FOR_SHARED_LIB) tmp_dir || \
		true; } && \
	for grade in $(ALL_LIBGRADES); do \
		if test "$$grade" != "$(GRADE)"; then \
			if mmake_grade_test c "$$grade"; then \
				$(MMAKE) GRADE=$$grade \
					mslice.depend || exit 1; \
				$(MMAKE) GRADE=$$grade \
					libmslice.install_gs_gas_files_c || \
					exit 1; \
			else \
				mmc --make --grade=$$grade \
					--install-prefix $(INSTALL_PREFIX) \
					libmslice.install_gs_gas || \
					exit 1; \
			fi; \
			for file in x $$grade_files; do \
				if test "$$file" != x; then \
					rm -f $$file; \
				fi; \
			done; \
			rm -f $(deps_subdir)$*.dep $(deps_subdir)$*.dv \
				*.$A *.$(EXT_FOR_SHARED_LIB) \
				*.jar *.dll *.err *.dep_err; \
		fi; \
	done && \
	for file in x $$grade_files; do \
		if test "$$file" != x; then \
			mv -f tmp_dir/`basename $$file` $$file \
				> /dev/null 2>&1; \
			true; \
		fi; \
	done && \
	{ mv -f tmp_dir/*.dep tmp_dir/*.dv $(deps_subdir).; \
		mv -f tmp_dir/* .; rmdir tmp_dir; true; }

# RULE install_ngs_pgs_files
.PHONY: libmslice.install_ngs_pgs_files
libmslice.install_ngs_pgs_files : \
		$(mslice.int0s) \
		$(mslice.ints) \
		$(mslice.int3s) \
		$(mslice.mhs)
	$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/int0s && \
	$(INSTALL) $(mslice.int0s) $(INSTALL_PREFIX)/MercurySystem/int0s
	$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/ints && \
	$(INSTALL) $(mslice.ints) $(INSTALL_PREFIX)/MercurySystem/ints
	$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/int3s && \
	$(INSTALL) $(mslice.int3s) $(INSTALL_PREFIX)/MercurySystem/int3s
	$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/mhs && \
	$(INSTALL) $(mslice.mhs) $(INSTALL_PREFIX)/MercurySystem/mhs

# RULE install_gs_gas_files
.PHONY: libmslice.install_gs_gas_files_c
libmslice.install_gs_gas_files_c : \
		$(mslice.mihs) \
		mslice.init \
		libmslice.$A \
		libmslice.$(EXT_FOR_SHARED_LIB)
	if mmake_grade_test hlc $(GRADE); then \
		$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/mihs/$(GRADESTRING) && \
		$(INSTALL) $(mslice.mihs) $(INSTALL_PREFIX)/MercurySystem/mihs/$(GRADESTRING) ; \
	fi
	if mmake_grade_test hlc $(GRADE); then \
		$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/inits/$(GRADESTRING) && \
		$(INSTALL) mslice.init $(INSTALL_PREFIX)/MercurySystem/inits/$(GRADESTRING) ; \
	fi
	if mmake_grade_test c $(GRADE); then \
		$(INSTALL_MKDIR) $(INSTALL_PREFIX)/MercurySystem/lib/$(GRADESTRING)/$(FULLARCH) && \
		$(INSTALL) libmslice.$A libmslice.$(EXT_FOR_SHARED_LIB) $(INSTALL_PREFIX)/MercurySystem/lib/$(GRADESTRING)/$(FULLARCH) ; \
	fi

# RULE collective_target_.check.errs
.PHONY: mslice.check
mslice.check : $(mslice.errs)

# RULE collective_target_.ints.dates
.PHONY: mslice.ints
mslice.ints : $(mslice.dates)

# RULE collective_target_.int3s.date3s
.PHONY: mslice.int3s
mslice.int3s : $(mslice.date3s)

# RULE collective_target_.opts.optdates
.PHONY: mslice.opts
mslice.opts : $(mslice.optdates)

# RULE collective_target_.trans_opts.trans_opt_dates
.PHONY: mslice.trans_opts
mslice.trans_opts : $(mslice.trans_opt_dates)

# RULE collective_target_.javas.all_javas
.PHONY: mslice.javas
mslice.javas : $(mslice.all_javas)

# RULE collective_target_.classes.classes
.PHONY: mslice.classes
mslice.classes : $(mslice.classes)

# RULE collective_target_.all_ints.dates
.PHONY: mslice.all_ints
mslice.all_ints : $(mslice.dates)

# RULE collective_target_.all_int3s.date3s
.PHONY: mslice.all_int3s
mslice.all_int3s : $(mslice.date3s)

# RULE collective_target_.all_opts.optdates
.PHONY: mslice.all_opts
mslice.all_opts : $(mslice.optdates)

# RULE collective_target_.all_trans_opts.trans_opt_dates
.PHONY: mslice.all_trans_opts
mslice.all_trans_opts : $(mslice.trans_opt_dates)

# RULE clean_local
clean_local : mslice.clean

# RULE clean_target
.PHONY: mslice.clean
mslice.clean :
	-echo $(mslice.dirs) | xargs rm -f
	-echo $(mslice.all_cs) | xargs rm -f
	-echo $(mslice.mihs) | xargs rm -f
	-echo $(mslice.all_os) | xargs rm -f
	-echo $(mslice.all_pic_os) | xargs rm -f
	-echo $(mslice.c_dates) | xargs rm -f
	-echo $(mslice.java_dates) | xargs rm -f
	-echo $(mslice.useds) | xargs rm -f
	-echo $(mslice.all_javas) | xargs rm -f
	-echo $(mslice.profs) | xargs rm -f
	-echo $(mslice.dep_errs) | xargs rm -f
	-echo $(mslice.errs) | xargs rm -f
	-echo $(mslice.foreign_cs) | xargs rm -f
	-rm -f mslice_init.c mslice_init.$O mslice_init.pic_o

# RULE realclean_local
realclean_local : mslice.realclean

# RULE realclean_target
.PHONY: mslice.realclean
mslice.realclean : mslice.clean
	-echo $(mslice.dates) | xargs rm -f
	-echo $(mslice.date0s) | xargs rm -f
	-echo $(mslice.date3s) | xargs rm -f
	-echo $(mslice.optdates) | xargs rm -f
	-echo $(mslice.trans_opt_dates) | xargs rm -f
	-echo $(mslice.ints) | xargs rm -f
	-echo $(mslice.int0s_to_clean) | xargs rm -f
	-echo $(mslice.int3s) | xargs rm -f
	-echo $(mslice.opts) | xargs rm -f
	-echo $(mslice.trans_opts) | xargs rm -f
	-echo $(mslice.analyses) | xargs rm -f
	-echo $(mslice.requests) | xargs rm -f
	-echo $(mslice.imdgs) | xargs rm -f
	-echo $(mslice.ds) | xargs rm -f
	-echo $(mslice.module_deps) | xargs rm -f
	-echo $(mslice.mhs_to_clean) | xargs rm -f
	-echo $(mslice.mihs_to_clean) | xargs rm -f
	-echo $(mslice.dlls) | xargs rm -f
	-echo $(mslice.foreign_dlls) | xargs rm -f
	-echo $(mslice.classes) | xargs rm -f
	-rm -f mslice$(EXT_FOR_EXE)  mslice.init libmslice.$A libmslice.$(EXT_FOR_SHARED_LIB) mslice.jar mslice.dep mslice.dv

