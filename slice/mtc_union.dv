ifeq (x,y)

# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for module `mtc_union'
# generated from source file `mtc_union.m'
# Generated by the Mercury compiler, version rotd-2017-07-15,
# configured for x86_64-apple-darwin13.4.0.


endif # ifeq(x,y)

# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for
# module `mtc_union' in source file `mtc_union.m'.
# Generated by Mercury compiler version rotd-2017-07-15
# configured for x86_64-apple-darwin13.4.0.

ifeq (x,y)

mtc_union.ms = \
	builtin_modules.m \
	feedback.m \
	feedback.automatic_parallelism.m \
	mdbcomp.m \
	mdbcomp.goal_path.m \
	mtc_union.m \
	prim_data.m \
	program_representation.m \
	rtti_access.m \
	shared_utilities.m \
	slice_and_dice.m \
	sym_name.m \
	trace_counts.m

endif # ifeq(x,y)

mtc_union.ms = \
	builtin_modules.m \
	feedback.m \
	feedback.automatic_parallelism.m \
	mdbcomp.m \
	mdbcomp.goal_path.m \
	mtc_union.m \
	prim_data.m \
	program_representation.m \
	rtti_access.m \
	shared_utilities.m \
	slice_and_dice.m \
	sym_name.m \
	trace_counts.m

ifeq (x,y)

mtc_union.errs = \
	builtin_modules.err \
	feedback.err \
	feedback.automatic_parallelism.err \
	mdbcomp.err \
	mdbcomp.goal_path.err \
	mtc_union.err \
	prim_data.err \
	program_representation.err \
	rtti_access.err \
	shared_utilities.err \
	slice_and_dice.err \
	sym_name.err \
	trace_counts.err

endif # ifeq(x,y)

mtc_union.errs = \
	builtin_modules.err \
	feedback.err \
	feedback.automatic_parallelism.err \
	mdbcomp.err \
	mdbcomp.goal_path.err \
	mtc_union.err \
	prim_data.err \
	program_representation.err \
	rtti_access.err \
	shared_utilities.err \
	slice_and_dice.err \
	sym_name.err \
	trace_counts.err

ifeq (x,y)

mtc_union.mods = \
	mdbcomp \
	mdbcomp.builtin_modules \
	mdbcomp.feedback \
	mdbcomp.feedback.automatic_parallelism \
	mdbcomp.goal_path \
	mdbcomp.prim_data \
	mdbcomp.program_representation \
	mdbcomp.rtti_access \
	mdbcomp.shared_utilities \
	mdbcomp.slice_and_dice \
	mdbcomp.sym_name \
	mdbcomp.trace_counts \
	mtc_union

endif # ifeq(x,y)

mtc_union.mods = \
	mdbcomp \
	mdbcomp.builtin_modules \
	mdbcomp.feedback \
	mdbcomp.feedback.automatic_parallelism \
	mdbcomp.goal_path \
	mdbcomp.prim_data \
	mdbcomp.program_representation \
	mdbcomp.rtti_access \
	mdbcomp.shared_utilities \
	mdbcomp.slice_and_dice \
	mdbcomp.sym_name \
	mdbcomp.trace_counts \
	mtc_union

ifeq (x,y)

mtc_union.parent_mods = \
	mdbcomp \
	mdbcomp.feedback

endif # ifeq(x,y)

mtc_union.parent_mods = \
	mdbcomp \
	mdbcomp.feedback

ifeq (x,y)

mtc_union.foreign =


endif # ifeq(x,y)

mtc_union.foreign =

ifeq (x,y)

mtc_union.foreign_cs = 

endif # ifeq(x,y)

mtc_union.foreign_cs =

ifeq (x,y)

mtc_union.foreign_dlls = $(mtc_union.foreign:%=$(dlls_subdir)%.dll)

endif # ifeq(x,y)

mtc_union.foreign_dlls = $(mtc_union.foreign:%=$(dlls_subdir)%.dll)

ifeq (x,y)

mtc_union.init_cs = $(mtc_union.mods:%=$(cs_subdir)%.c)

endif # ifeq(x,y)

mtc_union.init_cs = $(mtc_union.mods:%=$(cs_subdir)%.c)

ifeq (x,y)

mtc_union.cs = $(mtc_union.init_cs) 

endif # ifeq(x,y)

mtc_union.cs = $(mtc_union.init_cs)

ifeq (x,y)

mtc_union.dlls = $(mtc_union.mods:%=$(dlls_subdir)%.dll)

endif # ifeq(x,y)

mtc_union.dlls = $(mtc_union.mods:%=$(dlls_subdir)%.dll)

ifeq (x,y)

mtc_union.all_os = $(mtc_union.mods:%=$(os_subdir)%.$O)

endif # ifeq(x,y)

mtc_union.all_os = $(mtc_union.mods:%=$(os_subdir)%.$O)

ifeq (x,y)

mtc_union.all_pic_os = $(mtc_union.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

endif # ifeq(x,y)

mtc_union.all_pic_os = $(mtc_union.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

ifeq (x,y)

mtc_union.os = $(mtc_union.all_os)

endif # ifeq(x,y)

mtc_union.os = $(mtc_union.all_os)

ifeq (x,y)

mtc_union.pic_os = $(mtc_union.all_pic_os)

endif # ifeq(x,y)

mtc_union.pic_os = $(mtc_union.all_pic_os)

ifeq (x,y)

mtc_union.useds = $(mtc_union.mods:%=$(useds_subdir)%.used)

endif # ifeq(x,y)

mtc_union.useds = $(mtc_union.mods:%=$(used_subdir)%.used)

ifeq (x,y)

mtc_union.javas = $(mtc_union.mods:%=$(javas_subdir)%.java)

endif # ifeq(x,y)

mtc_union.javas = $(mtc_union.mods:%=$(javas_subdir)%.java)

ifeq (x,y)

mtc_union.classes = $(mtc_union.mods:%=$(classes_subdir)%.class) $(wildcard $(mtc_union.mods:%=$(classes_subdir)%\$$*.class))

endif # ifeq(x,y)

mtc_union.classes = \
	$(mtc_union.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mtc_union.mods:%=$(classes_subdir)%\$$*.class))

ifeq (x,y)

mtc_union.dirs = $(mtc_union.mods:%=$(dirs_subdir)%.dir)

endif # ifeq(x,y)

mtc_union.dirs = $(mtc_union.mods:%=$(dirs_subdir)%.dir)

ifeq (x,y)

mtc_union.dir_os = $(mtc_union.mods:%=$(dirs_subdir)%.dir/*.$O)

endif # ifeq(x,y)

mtc_union.dir_os = $(mtc_union.mods:%=$(dirs_subdir)%.dir/*.$O)

ifeq (x,y)

mtc_union.dates = $(mtc_union.mods:%=$(dates_subdir)%.date)

endif # ifeq(x,y)

mtc_union.dates = $(mtc_union.mods:%=$(dates_subdir)%.date)

ifeq (x,y)

mtc_union.date0s = $(mtc_union.mods:%=$(date0s_subdir)%.date0)

endif # ifeq(x,y)

mtc_union.date0s = $(mtc_union.mods:%=$(date0s_subdir)%.date0)

ifeq (x,y)

mtc_union.date3s = $(mtc_union.mods:%=$(date3s_subdir)%.date3)

endif # ifeq(x,y)

mtc_union.date3s = $(mtc_union.mods:%=$(date3s_subdir)%.date3)

ifeq (x,y)

mtc_union.optdates = $(mtc_union.mods:%=$(optdates_subdir)%.optdate)

endif # ifeq(x,y)

mtc_union.optdates = $(mtc_union.mods:%=$(optdates_subdir)%.optdate)

ifeq (x,y)

mtc_union.trans_opt_dates = $(mtc_union.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

endif # ifeq(x,y)

mtc_union.trans_opt_dates = $(mtc_union.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

ifeq (x,y)

mtc_union.c_dates = $(mtc_union.mods:%=$(c_dates_subdir)%.c_date)

endif # ifeq(x,y)

mtc_union.c_dates = $(mtc_union.mods:%=$(c_dates_subdir)%.c_date)

ifeq (x,y)

mtc_union.java_dates = $(mtc_union.mods:%=$(java_dates_subdir)%.java_date)

endif # ifeq(x,y)

mtc_union.java_dates = $(mtc_union.mods:%=$(java_dates_subdir)%.java_date)

ifeq (x,y)

mtc_union.ds = $(mtc_union.mods:%=$(ds_subdir)%.d)

endif # ifeq(x,y)

mtc_union.ds = $(mtc_union.mods:%=$(ds_subdir)%.d)

ifeq (x,y)

mtc_union.module_deps = $(mtc_union.mods:%=$(module_deps_subdir)%.module_dep)

endif # ifeq(x,y)

mtc_union.module_deps = $(mtc_union.mods:%=$(module_deps_subdir)%.module_dep)

ifeq (x,y)

mtc_union.mihs = $(mtc_union.mods:%=$(mihs_subdir)%.mih)

endif # ifeq(x,y)

mtc_union.mihs = $(mtc_union.mods:%=$(mihs_subdir)%.mih)

ifeq (x,y)

mtc_union.mhs = $(mtc_union.mods:%=%.mh)

endif # ifeq(x,y)

mtc_union.mhs = $(mtc_union.mods:%=%.mh)

ifeq (x,y)

mtc_union.all_mihs = $(mtc_union.mods:%=$(mihs_subdir)%.mih)

endif # ifeq(x,y)

mtc_union.all_mihs = $(mtc_union.mods:%=$(mihs_subdir)%.mih)

ifeq (x,y)

mtc_union.all_mhs = $(mtc_union.mods:%=%.mh)

endif # ifeq(x,y)

mtc_union.all_mhs = $(mtc_union.mods:%=%.mh)

ifeq (x,y)

mtc_union.ints = $(mtc_union.mods:%=$(ints_subdir)%.int) $(mtc_union.mods:%=$(int2s_subdir)%.int2)

endif # ifeq(x,y)

mtc_union.ints = \
	$(mtc_union.mods:%=$(ints_subdir)%.int) \
	$(mtc_union.mods:%=$(int2s_subdir)%.int2)

ifeq (x,y)

mtc_union.int0s = $(mtc_union.parent_mods:%=$(int0s_subdir)%.int0)

endif # ifeq(x,y)

mtc_union.int0s = $(mtc_union.parent_mods:%=$(int0s_subdir)%.int0)

ifeq (x,y)

mtc_union.all_int0s = $(mtc_union.mods:%=$(int0s_subdir)%.int0)

endif # ifeq(x,y)

mtc_union.all_int0s = $(mtc_union.mods:%=$(int0s_subdir)%.int0)

ifeq (x,y)

mtc_union.int3s = $(mtc_union.mods:%=$(int3s_subdir)%.int3)

endif # ifeq(x,y)

mtc_union.int3s = $(mtc_union.mods:%=$(int3s_subdir)%.int3)

ifeq (x,y)

mtc_union.opts = $(mtc_union.mods:%=$(opts_subdir)%.opt)

endif # ifeq(x,y)

mtc_union.opts = $(mtc_union.mods:%=$(opts_subdir)%.opt)

ifeq (x,y)

mtc_union.trans_opts = $(mtc_union.mods:%=$(trans_opts_subdir)%.trans_opt)

endif # ifeq(x,y)

mtc_union.trans_opts = $(mtc_union.mods:%=$(trans_opts_subdir)%.trans_opt)

ifeq (x,y)

mtc_union.analysiss = $(mtc_union.mods:%=$(analysiss_subdir)%.analysis)

endif # ifeq(x,y)

mtc_union.analysiss = $(mtc_union.mods:%=$(analysiss_subdir)%.analysis)

ifeq (x,y)

mtc_union.requests = $(mtc_union.mods:%=$(requests_subdir)%.request)

endif # ifeq(x,y)

mtc_union.requests = $(mtc_union.mods:%=$(requests_subdir)%.request)

ifeq (x,y)

mtc_union.imdgs = $(mtc_union.mods:%=$(imdgs_subdir)%.imdg)

endif # ifeq(x,y)

mtc_union.imdgs = $(mtc_union.mods:%=$(imdgs_subdir)%.imdg)

ifeq (x,y)

mtc_union.profs = $(mtc_union.mods:%=%.prof)


endif # ifeq(x,y)

mtc_union.profs = $(mtc_union.mods:%=%.prof)

ifeq (x,y)


endif # ifeq(x,y)

