:- module (ll_backend.frameopt).
:- interface.
:- type_representation((ll_backend.frameopt).det_frame_block_map, is_eqv_to(frame_block_map(det_entry_info, det_exit_info))).
:- type_representation((ll_backend.frameopt).frame_block_map(V_1, V_2), is_eqv_to(map(label, frame_block_info(V_1, V_2)))).
:- type_representation((ll_backend.frameopt).ord_needs_frame, is_eqv_to(map(label, block_needs_frame))).
:- type_representation((ll_backend.frameopt).pre_exit_dummy_label_map, is_eqv_to(map(label, label))).
:- type_representation((ll_backend.frameopt).pred_map, is_eqv_to(map(label, label))).
:- type_representation((ll_backend.frameopt).prop_queue, is_eqv_to(queue(label))).
:- type_representation((ll_backend.frameopt).rev_map, is_eqv_to(map(label, list(label)))).
