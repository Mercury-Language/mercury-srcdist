:- module (transform_hlds.tupling).
:- interface.
:- type_representation(candidate_headvar_origins, is_eqv_to(map.map(hlds.hlds_pred.pred_proc_id, parse_tree.prog_data.prog_var))).
:- type_representation(candidate_headvars, is_eqv_to(assoc_list.assoc_list(string, transform_hlds.tupling.candidate_headvar_origins))).
:- type_representation(transform_map, is_eqv_to(map.map(hlds.hlds_pred.pred_proc_id, transform_hlds.tupling.transformed_proc))).
:- type_representation(tupling_scheme, is_eqv_to(map.map(hlds.hlds_pred.pred_proc_id, transform_hlds.tupling.tupling_proposal))).
:- implementation.
:- type opt_tuple_alloc
    --->    opt_tuple_alloc .
