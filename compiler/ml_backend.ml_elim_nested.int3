:- module (ml_backend.ml_elim_nested).
:- interface.
:- import_module (libs.globals).
:- import_module (ml_backend.mlds).
:- type action
	where	type_is_abstract_enum(1).
:- inst (chain) == bound(chain_gc_stack_frames).
:- inst (hoist) == bound(hoist_nested_funcs).
