# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated module dependencies for
# module `hlds.make_hlds' in source file `make_hlds.m'.
# Generated by Mercury compiler version rotd-2023-04-04
# configured for x86_64-pc-linux-gnu.

# RULE date_file_deps
# target group names:
#   dates_and_err
# source group names:
#   (unnamed)
#   (unnamed)
#   ancestors
#   long deps
#   type_repn self dep
#   type_repn ancestor dep
hlds.make_hlds.optdate hlds.make_hlds.trans_opt_date make_hlds.err hlds.make_hlds.c_date hlds.make_hlds.java_date : \
		make_hlds.m \
	\
		hlds.make_hlds.int0 \
	\
		hlds.int0 \
	\
		builtin.int \
		private_builtin.int \
	\
		hlds.make_hlds.int \
	\
		hlds.int

# RULE objs_on_mihs
hlds.make_hlds.pic_o hlds.make_hlds.$O : \
		builtin.mih \
		hlds.mih \
		private_builtin.mih

# RULE mh_and_mih_on_c
hlds.make_hlds.mh hlds.make_hlds.mih : hlds.make_hlds.c

ifeq ($(findstring java,$(GRADE)),java)

# RULE module_dep_on_java
hlds.make_hlds.module_dep : jmercury/hlds__make_hlds.java

else

# RULE module_dep_on_c
hlds.make_hlds.module_dep : hlds.make_hlds.c

endif # conditional fragment

# RULE self_and_parent_date_deps
# source group names:
#   (unnamed)
#   ancestor int0
#   long dep int3s
hlds.make_hlds.date hlds.make_hlds.date0 hlds.date : \
		make_hlds.m \
	\
		hlds.int0 \
	\
		builtin.int3 \
		private_builtin.int3

# RULE self_and_parent_date0_deps
# target group names:
#   date0s
# source group names:
#   (unnamed)
#   long dep int3s
hlds.make_hlds.date0 hlds.date0 : \
		make_hlds.m \
	\
		builtin.int3 \
		private_builtin.int3

# RULE int0_on_date0
hlds.make_hlds.int0 : hlds.make_hlds.date0
	@:

# RULE int_on_date
hlds.make_hlds.int : hlds.make_hlds.date
	@:

# RULE int2_on_date
hlds.make_hlds.int2 : hlds.make_hlds.date
	@:

# RULE int3_on_date3
hlds.make_hlds.int3 : hlds.make_hlds.date3
	@:

# RULE opt_on_opt_date
hlds.make_hlds.opt : hlds.make_hlds.optdate
	@:

# RULE trans_opt_on_trans_opt_date
hlds.make_hlds.trans_opt : hlds.make_hlds.trans_opt_date
	@:

# RULE date0_on_src
hlds.make_hlds.date0 : make_hlds.m
	$(MCPI) $(ALL_GRADEFLAGS) $(ALL_MCPIFLAGS) hlds.make_hlds

# RULE date_on_src
hlds.make_hlds.date : make_hlds.m
	$(MCI) $(ALL_GRADEFLAGS) $(ALL_MCIFLAGS) hlds.make_hlds

# RULE date3_on_src
hlds.make_hlds.date3 : make_hlds.m
	$(MCSI) $(ALL_GRADEFLAGS) $(ALL_MCSIFLAGS) hlds.make_hlds

# RULE opt_date_on_src
hlds.make_hlds.optdate : make_hlds.m
	$(MCOI) $(ALL_GRADEFLAGS) $(ALL_MCOIFLAGS) hlds.make_hlds

# RULE trans_opt_date_on_src
hlds.make_hlds.trans_opt_date : make_hlds.m
	$(MCTOI) $(ALL_GRADEFLAGS) $(ALL_MCTOIFLAGS) hlds.make_hlds

# RULE c_date_on_src
hlds.make_hlds.c_date : make_hlds.m
	$(MCG) $(ALL_GRADEFLAGS) $(ALL_MCGFLAGS) hlds.make_hlds $(ERR_REDIRECT)

# RULE java_date_on_src
hlds.make_hlds.java_date : make_hlds.m
	$(MCG) $(ALL_GRADEFLAGS) $(ALL_MCGFLAGS) --java-only hlds.make_hlds $(ERR_REDIRECT)

