:- module (check_hlds.mode_constraints).
:- interface.
:- type_representation((check_hlds.mode_constraints).can_succeed, is_eqv_to(bool)).
:- type_representation((check_hlds.mode_constraints).constrain_var, is_eqv_to(pred(prog_var, mode_constraint, mode_constraint, mode_constraint_info, mode_constraint_info))).
:- type_representation((check_hlds.mode_constraints).ho_calls, is_eqv_to(multi_map(prog_var_and_level, pair(goal_id, list(prog_var))))).
:- type_representation((check_hlds.mode_constraints).ho_modes, is_eqv_to(multi_map(prog_var_and_level, list(mer_mode)))).
:- type_representation((check_hlds.mode_constraints).sccs, is_eqv_to(list(list(pred_id)))).
