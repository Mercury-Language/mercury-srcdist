:- module (check_hlds.mcsolver).
:- interface.
:- use_module list.
:- use_module (check_hlds.abstract_mode_constraints).
:- type mcvar == check_hlds.abstract_mode_constraints.mc_var.
:- type mcvars == list.list(check_hlds.mcsolver.mcvar).
:- type prep_cstrts.
:- type solver_cstrts.
:- type_representation((check_hlds.mcsolver).assgts, is_eqv_to(list.list(check_hlds.mcsolver.assgt))).
:- type_representation((check_hlds.mcsolver).complex_cstrt_map, is_eqv_to(multi_map.multi_map(check_hlds.mcsolver.mcvar, check_hlds.mcsolver.complex_cstrt))).
:- type_representation((check_hlds.mcsolver).complex_cstrts, is_eqv_to(list.list(check_hlds.mcsolver.complex_cstrt))).
:- type_representation((check_hlds.mcsolver).eqv_vars, is_eqv_to(eqvclass.eqvclass(check_hlds.mcsolver.mcvar))).
:- type_representation((check_hlds.mcsolver).impls, is_eqv_to(list.list(check_hlds.mcsolver.impl))).
:- type_representation((check_hlds.mcsolver).mcvar, is_eqv_to(check_hlds.abstract_mode_constraints.mc_var)).
:- type_representation((check_hlds.mcsolver).mcvars, is_eqv_to(list.list(check_hlds.mcsolver.mcvar))).
:- type_representation((check_hlds.mcsolver).prep_cstrts, is_word_aligned_ptr).
:- type_representation((check_hlds.mcsolver).solver_cstrts, is_word_aligned_ptr).
