:- module (transform_hlds.goal_store).
:- interface.
:- type goal_store(T).
:- type stored_goal
    --->    stored_goal(hlds.hlds_goal.hlds_goal, hlds.instmap.instmap).
:- type_representation((transform_hlds.goal_store).goal_store(V_1), is_eqv_to(map.map(V_1, transform_hlds.goal_store.stored_goal))).
:- type_representation((transform_hlds.goal_store).stored_goal, is_word_aligned_ptr).
:- implementation.
:- type goal_store(T) == map.map(T, transform_hlds.goal_store.stored_goal).
