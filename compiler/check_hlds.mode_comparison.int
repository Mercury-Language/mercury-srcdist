:- module (check_hlds.mode_comparison).
:- interface.
:- import_module hlds.
:- import_module list.
:- import_module parse_tree.
:- import_module (check_hlds.mode_info).
:- import_module (hlds.hlds_module).
:- import_module (hlds.hlds_pred).
:- import_module (parse_tree.prog_data).
:- type match
    --->    better 
    ;       worse 
    ;       same 
    ;       incomparable .
:- type proc_mode
    --->    proc_mode(hlds.hlds_pred.proc_id, parse_tree.prog_data.inst_var_sub, list.list(parse_tree.prog_data.mer_mode)).
:- pred choose_best_match(check_hlds.mode_info.mode_info, list.list(check_hlds.mode_comparison.proc_mode), hlds.hlds_pred.pred_id, hlds.hlds_pred.proc_table, list.list(parse_tree.prog_data.prog_var), hlds.hlds_pred.proc_id, parse_tree.prog_data.inst_var_sub, list.list(parse_tree.prog_data.mer_mode)).
:- mode choose_best_match((builtin.in), (builtin.in), (builtin.in), (builtin.in), (builtin.in), (builtin.out), (builtin.out), (builtin.out)) is det.
:- pred modes_are_identical_bar_cc(hlds.hlds_pred.proc_id, hlds.hlds_pred.proc_id, hlds.hlds_pred.pred_info, hlds.hlds_module.module_info).
:- mode modes_are_identical_bar_cc((builtin.in), (builtin.in), (builtin.in), (builtin.in)) is semidet.
:- pred modes_are_indistinguishable(hlds.hlds_pred.proc_id, hlds.hlds_pred.proc_id, hlds.hlds_pred.pred_info, hlds.hlds_module.module_info).
:- mode modes_are_indistinguishable((builtin.in), (builtin.in), (builtin.in), (builtin.in)) is semidet.
