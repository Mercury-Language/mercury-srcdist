:- module (transform_hlds.unneeded_code).
:- interface.
:- type_representation(refined_goal_map, is_eqv_to(map.map(pair.pair(mdbcomp.goal_path.goal_id, int), list.list(hlds.hlds_goal.hlds_goal)))).
:- type_representation(where_needed_branches, is_eqv_to(map.map(transform_hlds.unneeded_code.branch_point, set.set(int)))).
:- type_representation(where_needed_map, is_eqv_to(map.map(parse_tree.prog_data.prog_var, transform_hlds.unneeded_code.where_needed))).
