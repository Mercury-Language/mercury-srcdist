# vim: ts=8 sw=8 noexpandtab ft=make

# Automatically generated dependency variables for
# module `mer_browser' in source file `mer_browser.m'.
# Generated by Mercury compiler version rotd-2024-09-23
# configured for x86_64-pc-linux-gnu.

mer_browser.ms = \
	browse.m \
	browser_info.m \
	browser_term.m \
	collect_lib.m \
	cterm.m \
	debugger_interface.m \
	declarative_analyser.m \
	declarative_debugger.m \
	declarative_edt.m \
	declarative_execution.m \
	declarative_oracle.m \
	declarative_tree.m \
	declarative_user.m \
	diff.m \
	dl.m \
	frame.m \
	help.m \
	interactive_query.m \
	io_action.m \
	listing.m \
	mdb.m \
	mer_browser.m \
	name_mangle.m \
	parse.m \
	percent_encoding.m \
	print_term.m \
	save_term.m \
	sized_pretty.m \
	term_paths.m \
	term_rep.m \
	term_to_html.m \
	util.m

mer_browser.dep_errs = \
	browse.dep_err \
	browser_info.dep_err \
	browser_term.dep_err \
	collect_lib.dep_err \
	cterm.dep_err \
	debugger_interface.dep_err \
	declarative_analyser.dep_err \
	declarative_debugger.dep_err \
	declarative_edt.dep_err \
	declarative_execution.dep_err \
	declarative_oracle.dep_err \
	declarative_tree.dep_err \
	declarative_user.dep_err \
	diff.dep_err \
	dl.dep_err \
	frame.dep_err \
	help.dep_err \
	interactive_query.dep_err \
	io_action.dep_err \
	listing.dep_err \
	mdb.dep_err \
	mer_browser.dep_err \
	name_mangle.dep_err \
	parse.dep_err \
	percent_encoding.dep_err \
	print_term.dep_err \
	save_term.dep_err \
	sized_pretty.dep_err \
	term_paths.dep_err \
	term_rep.dep_err \
	term_to_html.dep_err \
	util.dep_err

mer_browser.errs = \
	browse.err \
	browser_info.err \
	browser_term.err \
	collect_lib.err \
	cterm.err \
	debugger_interface.err \
	declarative_analyser.err \
	declarative_debugger.err \
	declarative_edt.err \
	declarative_execution.err \
	declarative_oracle.err \
	declarative_tree.err \
	declarative_user.err \
	diff.err \
	dl.err \
	frame.err \
	help.err \
	interactive_query.err \
	io_action.err \
	listing.err \
	mdb.err \
	mer_browser.err \
	name_mangle.err \
	parse.err \
	percent_encoding.err \
	print_term.err \
	save_term.err \
	sized_pretty.err \
	term_paths.err \
	term_rep.err \
	term_to_html.err \
	util.err

mer_browser.mods = \
	mdb \
	mdb.browse \
	mdb.browser_info \
	mdb.browser_term \
	mdb.collect_lib \
	mdb.cterm \
	mdb.debugger_interface \
	mdb.declarative_analyser \
	mdb.declarative_debugger \
	mdb.declarative_edt \
	mdb.declarative_execution \
	mdb.declarative_oracle \
	mdb.declarative_tree \
	mdb.declarative_user \
	mdb.diff \
	mdb.dl \
	mdb.frame \
	mdb.help \
	mdb.interactive_query \
	mdb.io_action \
	mdb.listing \
	mdb.name_mangle \
	mdb.parse \
	mdb.percent_encoding \
	mdb.print_term \
	mdb.save_term \
	mdb.sized_pretty \
	mdb.term_paths \
	mdb.term_rep \
	mdb.term_to_html \
	mdb.util \
	mer_browser

mer_browser.parent_mods = mdb

mer_browser.ds = $(mer_browser.mods:%=$(ds_subdir)%.d)

mer_browser.module_deps = $(mer_browser.mods:%=$(module_deps_subdir)%.module_dep)

mer_browser.profs = $(mer_browser.mods:%=%.prof)

mer_browser.ints = \
	$(mer_browser.mods:%=$(ints_subdir)%.int) \
	$(mer_browser.mods:%=$(int2s_subdir)%.int2)

mer_browser.int0s = $(mer_browser.parent_mods:%=$(int0s_subdir)%.int0)

mer_browser.int0s_to_clean = $(mer_browser.mods:%=$(int0s_subdir)%.int0)

mer_browser.all_int0s = $(mer_browser.mods:%=$(int0s_subdir)%.int0)

mer_browser.int3s = $(mer_browser.mods:%=$(int3s_subdir)%.int3)

mer_browser.dates = $(mer_browser.mods:%=$(dates_subdir)%.date)

mer_browser.date0s = $(mer_browser.mods:%=$(date0s_subdir)%.date0)

mer_browser.date3s = $(mer_browser.mods:%=$(date3s_subdir)%.date3)

mer_browser.all_opts = $(mer_browser.mods:%=$(opts_subdir)%.opt)

mer_browser.opts = $(mer_browser.mods:%=$(opts_subdir)%.opt)

mer_browser.all_trans_opts = $(mer_browser.mods:%=$(trans_opts_subdir)%.trans_opt)

mer_browser.trans_opts = $(mer_browser.mods:%=$(trans_opts_subdir)%.trans_opt)

mer_browser.optdates = $(mer_browser.mods:%=$(optdates_subdir)%.optdate)

mer_browser.trans_opt_dates = $(mer_browser.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

mer_browser.all_cs = $(mer_browser.mods:%=$(cs_subdir)%.c)

mer_browser.init_cs = $(mer_browser.mods:%=$(cs_subdir)%.c)

mer_browser.c_dates = $(mer_browser.mods:%=$(c_dates_subdir)%.c_date)

mer_browser.all_os = $(mer_browser.mods:%=$(os_subdir)%.$O)

mer_browser.all_pic_os = $(mer_browser.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

mer_browser.foreign =

mer_browser.foreign_cs =

mer_browser.mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

mer_browser.mhs = $(mer_browser.mods:%=$(mhs_subdir)%.mh)

mer_browser.mihs_to_clean = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

mer_browser.all_mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

mer_browser.mhs_to_clean = $(mer_browser.mods:%=$(mhs_subdir)%.mh)

mer_browser.all_mhs = $(mer_browser.mods:%=$(mhs_subdir)%.mh)

mer_browser.all_javas = $(mer_browser.mods:%=$(javas_subdir)%.java)

mer_browser.java_dates = $(mer_browser.mods:%=$(java_dates_subdir)%.java_date)

ifeq ($(findstring java,$(GRADE)),java)

mer_browser.classes = \
	$(mer_browser.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mer_browser.mods:%=$(classes_subdir)%\$$*.class))

else

mer_browser.classes = 

endif # conditional fragment

mer_browser.all_css = $(mer_browser.mods:%=$(css_subdir)%.cs)

mer_browser.cs_dates = $(mer_browser.mods:%=$(cs_dates_subdir)%.cs_date)

mer_browser.dlls = $(mer_browser.mods:%=$(dlls_subdir)%.dll)

mer_browser.foreign_dlls = $(mer_browser.foreign:%=$(dlls_subdir)%.dll)

mer_browser.useds = $(mer_browser.mods:%=$(useds_subdir)%.used)

mer_browser.analyses = $(mer_browser.mods:%=$(analyses_subdir)%.analysis)

mer_browser.requests = $(mer_browser.mods:%=$(requests_subdir)%.request)

mer_browser.imdgs = $(mer_browser.mods:%=$(imdgs_subdir)%.imdg)

