# vim: ts=8 sw=8 noexpandtab ft=make

ifeq (x,y)

# Automatically generated dependency variables for module `mer_browser'
# generated from source file `mer_browser.m'
# Generated by the Mercury compiler, version rotd-2017-07-04,
# configured for x86_64-apple-darwin13.4.0.


endif # ifeq(x,y)

# Automatically generated dependency variables for module `mer_browser'
# generated from source file `mer_browser.m'
# Generated by the Mercury compiler, version rotd-2017-07-04,
# configured for x86_64-apple-darwin13.4.0.

ifeq (x,y)

mer_browser.ms = \
	browse.m \
	browser_info.m \
	browser_term.m \
	collect_lib.m \
	cterm.m \
	debugger_interface.m \
	declarative_analyser.m \
	declarative_debugger.m \
	declarative_edt.m \
	declarative_execution.m \
	declarative_oracle.m \
	declarative_tree.m \
	declarative_user.m \
	diff.m \
	dl.m \
	frame.m \
	help.m \
	interactive_query.m \
	io_action.m \
	listing.m \
	mdb.m \
	mer_browser.m \
	name_mangle.m \
	parse.m \
	sized_pretty.m \
	term_rep.m \
	util.m

endif # ifeq(x,y)

mer_browser.ms = \
	browse.m \
	browser_info.m \
	browser_term.m \
	collect_lib.m \
	cterm.m \
	debugger_interface.m \
	declarative_analyser.m \
	declarative_debugger.m \
	declarative_edt.m \
	declarative_execution.m \
	declarative_oracle.m \
	declarative_tree.m \
	declarative_user.m \
	diff.m \
	dl.m \
	frame.m \
	help.m \
	interactive_query.m \
	io_action.m \
	listing.m \
	mdb.m \
	mer_browser.m \
	name_mangle.m \
	parse.m \
	sized_pretty.m \
	term_rep.m \
	util.m

ifeq (x,y)

mer_browser.errs = \
	browse.err \
	browser_info.err \
	browser_term.err \
	collect_lib.err \
	cterm.err \
	debugger_interface.err \
	declarative_analyser.err \
	declarative_debugger.err \
	declarative_edt.err \
	declarative_execution.err \
	declarative_oracle.err \
	declarative_tree.err \
	declarative_user.err \
	diff.err \
	dl.err \
	frame.err \
	help.err \
	interactive_query.err \
	io_action.err \
	listing.err \
	mdb.err \
	mer_browser.err \
	name_mangle.err \
	parse.err \
	sized_pretty.err \
	term_rep.err \
	util.err

endif # ifeq(x,y)

mer_browser.errs = \
	browse.err \
	browser_info.err \
	browser_term.err \
	collect_lib.err \
	cterm.err \
	debugger_interface.err \
	declarative_analyser.err \
	declarative_debugger.err \
	declarative_edt.err \
	declarative_execution.err \
	declarative_oracle.err \
	declarative_tree.err \
	declarative_user.err \
	diff.err \
	dl.err \
	frame.err \
	help.err \
	interactive_query.err \
	io_action.err \
	listing.err \
	mdb.err \
	mer_browser.err \
	name_mangle.err \
	parse.err \
	sized_pretty.err \
	term_rep.err \
	util.err

ifeq (x,y)

mer_browser.mods = \
	mdb \
	mdb.browse \
	mdb.browser_info \
	mdb.browser_term \
	mdb.collect_lib \
	mdb.cterm \
	mdb.debugger_interface \
	mdb.declarative_analyser \
	mdb.declarative_debugger \
	mdb.declarative_edt \
	mdb.declarative_execution \
	mdb.declarative_oracle \
	mdb.declarative_tree \
	mdb.declarative_user \
	mdb.diff \
	mdb.dl \
	mdb.frame \
	mdb.help \
	mdb.interactive_query \
	mdb.io_action \
	mdb.listing \
	mdb.name_mangle \
	mdb.parse \
	mdb.sized_pretty \
	mdb.term_rep \
	mdb.util \
	mer_browser

endif # ifeq(x,y)

mer_browser.mods = \
	mdb \
	mdb.browse \
	mdb.browser_info \
	mdb.browser_term \
	mdb.collect_lib \
	mdb.cterm \
	mdb.debugger_interface \
	mdb.declarative_analyser \
	mdb.declarative_debugger \
	mdb.declarative_edt \
	mdb.declarative_execution \
	mdb.declarative_oracle \
	mdb.declarative_tree \
	mdb.declarative_user \
	mdb.diff \
	mdb.dl \
	mdb.frame \
	mdb.help \
	mdb.interactive_query \
	mdb.io_action \
	mdb.listing \
	mdb.name_mangle \
	mdb.parse \
	mdb.sized_pretty \
	mdb.term_rep \
	mdb.util \
	mer_browser

ifeq (x,y)

mer_browser.parent_mods = \
	mdb

endif # ifeq(x,y)

mer_browser.parent_mods = mdb

ifeq (x,y)

mer_browser.foreign =


endif # ifeq(x,y)

mer_browser.foreign =

ifeq (x,y)

mer_browser.foreign_cs = 

endif # ifeq(x,y)

mer_browser.foreign_cs =

ifeq (x,y)

mer_browser.foreign_dlls = $(mer_browser.foreign:%=$(dlls_subdir)%.dll)

endif # ifeq(x,y)

mer_browser.foreign_dlls = $(mer_browser.foreign:%=$(dlls_subdir)%.dll)

ifeq (x,y)

mer_browser.init_cs = $(mer_browser.mods:%=$(cs_subdir)%.c)

endif # ifeq(x,y)

mer_browser.init_cs = $(mer_browser.mods:%=$(cs_subdir)%.c)

ifeq (x,y)

mer_browser.cs = $(mer_browser.init_cs) 

endif # ifeq(x,y)

mer_browser.cs = $(mer_browser.init_cs)

ifeq (x,y)

mer_browser.dlls = $(mer_browser.mods:%=$(dlls_subdir)%.dll)

endif # ifeq(x,y)

mer_browser.dlls = $(mer_browser.mods:%=$(dlls_subdir)%.dll)

ifeq (x,y)

mer_browser.all_os = $(mer_browser.mods:%=$(os_subdir)%.$O)

endif # ifeq(x,y)

mer_browser.all_os = $(mer_browser.mods:%=$(os_subdir)%.$O)

ifeq (x,y)

mer_browser.all_pic_os = $(mer_browser.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

endif # ifeq(x,y)

mer_browser.all_pic_os = $(mer_browser.mods:%=$(os_subdir)%.$(EXT_FOR_PIC_OBJECTS))

ifeq (x,y)

mer_browser.os = $(mer_browser.all_os)

endif # ifeq(x,y)

mer_browser.os = $(mer_browser.all_os)

ifeq (x,y)

mer_browser.pic_os = $(mer_browser.all_pic_os)

endif # ifeq(x,y)

mer_browser.pic_os = $(mer_browser.all_pic_os)

ifeq (x,y)

mer_browser.useds = $(mer_browser.mods:%=$(useds_subdir)%.used)

endif # ifeq(x,y)

mer_browser.useds = $(mer_browser.mods:%=$(used_subdir)%.used)

ifeq (x,y)

mer_browser.javas = $(mer_browser.mods:%=$(javas_subdir)%.java)

endif # ifeq(x,y)

mer_browser.javas = $(mer_browser.mods:%=$(javas_subdir)%.java)

ifeq (x,y)

mer_browser.classes = $(mer_browser.mods:%=$(classes_subdir)%.class) $(wildcard $(mer_browser.mods:%=$(classes_subdir)%\$$*.class))

endif # ifeq(x,y)

mer_browser.classes = \
	$(mer_browser.mods:%=$(classes_subdir)%.class) \
	$(wildcard $(mer_browser.mods:%=$(classes_subdir)%\$$*.class))

ifeq (x,y)

mer_browser.dirs = $(mer_browser.mods:%=$(dirs_subdir)%.dir)

endif # ifeq(x,y)

mer_browser.dirs = $(mer_browser.mods:%=$(dirs_subdir)%.dir)

ifeq (x,y)

mer_browser.dir_os = $(mer_browser.mods:%=$(dirs_subdir)%.dir/*.$O)

endif # ifeq(x,y)

mer_browser.dir_os = $(mer_browser.mods:%=$(dirs_subdir)%.dir/*.$O)

ifeq (x,y)

mer_browser.dates = $(mer_browser.mods:%=$(dates_subdir)%.date)

endif # ifeq(x,y)

mer_browser.dates = $(mer_browser.mods:%=$(dates_subdir)%.date)

ifeq (x,y)

mer_browser.date0s = $(mer_browser.mods:%=$(date0s_subdir)%.date0)

endif # ifeq(x,y)

mer_browser.date0s = $(mer_browser.mods:%=$(date0s_subdir)%.date0)

ifeq (x,y)

mer_browser.date3s = $(mer_browser.mods:%=$(date3s_subdir)%.date3)

endif # ifeq(x,y)

mer_browser.date3s = $(mer_browser.mods:%=$(date3s_subdir)%.date3)

ifeq (x,y)

mer_browser.optdates = $(mer_browser.mods:%=$(optdates_subdir)%.optdate)

endif # ifeq(x,y)

mer_browser.optdates = $(mer_browser.mods:%=$(optdates_subdir)%.optdate)

ifeq (x,y)

mer_browser.trans_opt_dates = $(mer_browser.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

endif # ifeq(x,y)

mer_browser.trans_opt_dates = $(mer_browser.mods:%=$(trans_opt_dates_subdir)%.trans_opt_date)

ifeq (x,y)

mer_browser.c_dates = $(mer_browser.mods:%=$(c_dates_subdir)%.c_date)

endif # ifeq(x,y)

mer_browser.c_dates = $(mer_browser.mods:%=$(c_dates_subdir)%.c_date)

ifeq (x,y)

mer_browser.java_dates = $(mer_browser.mods:%=$(java_dates_subdir)%.java_date)

endif # ifeq(x,y)

mer_browser.java_dates = $(mer_browser.mods:%=$(java_dates_subdir)%.java_date)

ifeq (x,y)

mer_browser.ds = $(mer_browser.mods:%=$(ds_subdir)%.d)

endif # ifeq(x,y)

mer_browser.ds = $(mer_browser.mods:%=$(ds_subdir)%.d)

ifeq (x,y)

mer_browser.module_deps = $(mer_browser.mods:%=$(module_deps_subdir)%.module_dep)

endif # ifeq(x,y)

mer_browser.module_deps = $(mer_browser.mods:%=$(module_deps_subdir)%.module_dep)

ifeq (x,y)

mer_browser.mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

endif # ifeq(x,y)

mer_browser.mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

ifeq (x,y)

mer_browser.mhs = $(mer_browser.mods:%=%.mh)

endif # ifeq(x,y)

mer_browser.mhs = $(mer_browser.mods:%=%.mh)

ifeq (x,y)

mer_browser.all_mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

endif # ifeq(x,y)

mer_browser.all_mihs = $(mer_browser.mods:%=$(mihs_subdir)%.mih)

ifeq (x,y)

mer_browser.all_mhs = $(mer_browser.mods:%=%.mh)

endif # ifeq(x,y)

mer_browser.all_mhs = $(mer_browser.mods:%=%.mh)

ifeq (x,y)

mer_browser.ints = $(mer_browser.mods:%=$(ints_subdir)%.int) $(mer_browser.mods:%=$(int2s_subdir)%.int2)

endif # ifeq(x,y)

mer_browser.ints = \
	$(mer_browser.mods:%=$(ints_subdir)%.int) \
	$(mer_browser.mods:%=$(int2s_subdir)%.int2)

ifeq (x,y)

mer_browser.int0s = $(mer_browser.parent_mods:%=$(int0s_subdir)%.int0)

endif # ifeq(x,y)

mer_browser.int0s = $(mer_browser.parent_mods:%=$(int0s_subdir)%.int0)

ifeq (x,y)

mer_browser.all_int0s = $(mer_browser.mods:%=$(int0s_subdir)%.int0)

endif # ifeq(x,y)

mer_browser.all_int0s = $(mer_browser.mods:%=$(int0s_subdir)%.int0)

ifeq (x,y)

mer_browser.int3s = $(mer_browser.mods:%=$(int3s_subdir)%.int3)

endif # ifeq(x,y)

mer_browser.int3s = $(mer_browser.mods:%=$(int3s_subdir)%.int3)

ifeq (x,y)

mer_browser.opts = $(mer_browser.mods:%=$(opts_subdir)%.opt)

endif # ifeq(x,y)

mer_browser.opts = $(mer_browser.mods:%=$(opts_subdir)%.opt)

ifeq (x,y)

mer_browser.trans_opts = $(mer_browser.mods:%=$(trans_opts_subdir)%.trans_opt)

endif # ifeq(x,y)

mer_browser.trans_opts = $(mer_browser.mods:%=$(trans_opts_subdir)%.trans_opt)

ifeq (x,y)

mer_browser.analysiss = $(mer_browser.mods:%=$(analysiss_subdir)%.analysis)

endif # ifeq(x,y)

mer_browser.analysiss = $(mer_browser.mods:%=$(analysiss_subdir)%.analysis)

ifeq (x,y)

mer_browser.requests = $(mer_browser.mods:%=$(requests_subdir)%.request)

endif # ifeq(x,y)

mer_browser.requests = $(mer_browser.mods:%=$(requests_subdir)%.request)

ifeq (x,y)

mer_browser.imdgs = $(mer_browser.mods:%=$(imdgs_subdir)%.imdg)

endif # ifeq(x,y)

mer_browser.imdgs = $(mer_browser.mods:%=$(imdgs_subdir)%.imdg)

ifeq (x,y)

mer_browser.profs = $(mer_browser.mods:%=%.prof)


endif # ifeq(x,y)

mer_browser.profs = $(mer_browser.mods:%=%.prof)

ifeq (x,y)


endif # ifeq(x,y)

